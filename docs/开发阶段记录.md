# 开发阶段记录

## 项目原则

- 每个阶段都要有明确交付物。
- 每个阶段都要记录验收结果。
- 每个阶段完成后都要 Git 提交。
- 新会话开始前先读 `docs/README.md` 和本文件。
- 开始代码模块前必须先确认当前阶段文档状态。

## 阶段 0 - 文档与架构基线

日期：2026-06-28

状态：已完成

### 输入

- 用户目标：完成个人作品集搭建。
- 内容来源：简历、RAG 多模态导购项目、竞品分析 Agent 协作系统、GitHub。
- 参考站：`https://steven.com/`。
- 用户确认：
  - 主叙事选择 AI PM 求职和海外 AI 研究生申请均衡。
  - 双语范围选择项目介绍双语。
  - 第一阶段选择文档与架构基线。
  - 采用推荐方案 1：文档基线 + 项目骨架设计。

### 已完成

- 检查当前仓库状态：当前目录为 `/Users/tongjiarui/Documents/作品集`，仓库无历史提交。
- 确认旧记忆中的 `.worktrees/portfolio-site` 当前不存在。
- 检查本地字体资源：Field Gothic TEST 和 Northwell 可用。
- 使用 Chrome 读取 steven.com：
  - 首页。
  - 右侧菜单。
  - Creator Media 内容页。
  - Join Us 页面。
  - 导航、字体、颜色、预加载和滚动后信息区。
- 读取简历 PDF 文本。
- 读取 RAG 多模态导购项目 README。
- 读取 VibeCoding 需求验证 Skill PRD。
- 读取 GitHub profile 和 repositories 页面公开信息。
- 创建中文文档基线：
  - `docs/README.md`
  - `docs/目标站拆解.md`
  - `docs/信息架构.md`
  - `docs/技术方案.md`
  - `docs/开发阶段记录.md`

### 验收标准

- `docs/README.md` 可以作为新会话入口。
- `docs/目标站拆解.md` 覆盖页面、组件、字体、颜色、动效和版权边界。
- `docs/信息架构.md` 覆盖内容源、页面映射、双语策略和项目模板。
- `docs/技术方案.md` 覆盖推荐技术栈、目录规划、核心模块、动效边界和测试策略。
- `docs/开发阶段记录.md` 记录当前阶段完成事项、验收结果和下一阶段计划。
- Git 提交信息说明阶段 0 完成内容。

### 验收结果

自审通过。

- 文档入口完整，`docs/README.md` 可作为新会话接手入口。
- 目标站拆解覆盖首页、菜单、内容页、Join Us、字体、颜色和动效。
- 信息架构覆盖双语策略、页面映射、项目模板和内容可信度规则。
- 技术方案覆盖 React/Vite 路线、目录规划、核心模块、动效边界和测试策略。
- 未发现空白占坑、未定义范围或互相矛盾的阶段要求。

### 下一阶段建议

阶段 1 - 项目脚手架与基础质量门：

- 初始化 Vite + React + TypeScript。
- 建立 `.nvmrc`，目标 Node 版本按本机可用运行时确认。
- 配置 ESLint、Vitest、Testing Library。
- 建立 `src/content` 的项目数据结构，先写内容映射测试。
- 建立路由骨架和空页面。
- 建立基础 CSS reset、tokens、fonts 文件。
- 验证 `npm run test`、`npm run lint`、`npm run build`。
- 阶段完成后 Git 提交。

## 阶段 1 - 项目脚手架与基础质量门

状态：未开始

目标：

- 建立可运行的 React/Vite 项目。
- 把后续视觉实现放进稳定目录结构。
- 从第一段生产代码开始建立测试。

验收：

- 本地 dev server 可启动。
- 基础路由可渲染。
- 内容数据结构有测试。
- build、lint、test 通过。

## 阶段 2 - 视觉系统与首页首屏

状态：未开始

目标：

- 引入字体、tokens、reset。
- 实现 fixed nav、菜单按钮、右下 theme toggle。
- 实现首页首屏布局和简化系统环视觉。

验收：

- 桌面首屏接近 steven.com 的空间比例。
- 移动端不溢出。
- 字体加载失败时有可接受 fallback。
- 不出现蓝紫渐变、emoji 图标、圆角卡片模板感。

## 阶段 3 - 菜单、预加载和页面转场

状态：未开始

目标：

- 实现右侧菜单覆盖层。
- 实现深色预加载过场。
- 实现路由切换动效。
- 接入 reduced motion fallback。

验收：

- 菜单打开和关闭可用。
- 键盘和屏幕阅读器可识别按钮。
- reduced motion 下动画降级。
- 页面切换不卡死。

## 阶段 4 - 项目页双语内容

状态：未开始

目标：

- 完成 RAG Agent、VibeCoding Skill、YOLO11-OBB 三个项目页。
- 每个项目页包含中文和英文说明。
- 指标、复盘和外链来自已验证内容源。

验收：

- 每个项目页结构一致但视觉不死板。
- 不编造数字和成果。
- 中英文内容语义一致，但不机械直译。

## 阶段 5 - 视觉资产与滚动叙事增强

状态：未开始

目标：

- 为首页和项目页生成或制作对应视觉资产。
- 增强滚动信息区、masonry、next project 过渡。
- 逐步接近目标站的完整视觉体验。

验收：

- 资产合法可用。
- 首屏和项目页都有真实视觉资产。
- Lighthouse 和浏览器手动检查无明显问题。

## 阶段 6 - 内容打磨、部署和最终验收

状态：未开始

目标：

- 完成文案打磨。
- 检查移动端、性能、可访问性、SEO。
- 部署并记录上线方式。

验收：

- build 通过。
- 主要页面视觉验收通过。
- 链接可用。
- 文档记录最终状态。
